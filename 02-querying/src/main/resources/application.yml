server:
  port: 8080

spring:
  application:
    name: 02-querying
  data:
    mongodb:
      uri: mongodb://localhost:27017/querying_db

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
