server:
  port: 8080

spring:
  application:
    name: 07-schema-validation
  data:
    mongodb:
      uri: mongodb://localhost:27017/schemavalidation_db

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
