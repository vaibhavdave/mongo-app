server:
  port: 8080

spring:
  application:
    name: 05-aggregation
  data:
    mongodb:
      uri: mongodb://localhost:27017/aggregation_db

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
